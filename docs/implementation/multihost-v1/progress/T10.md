# T10 진행 기록

- 상태: done
- 구현: canonical `run --pipeline market|self --mode host|headless --workspace --hours --schedule-key --json`를 추가했다. CLI가 v1 RunSpec과 deterministic jobs를 RunStore에 기록하고 CREATED→PREPARED로 전환한 뒤 host는 prepare/validate, headless는 execute next action만 반환하며 subprocess를 직접 호출하지 않는다.
- schedule_key duplicate creation now returns structured `SCHEDULE_SLOT_EXISTS` rather than raw SQLite error.
- Added `ingest --state-dir --run-id --job-id --result`, which validates the persisted request hash before recording an immutable attempt and completing only that job.
- Added canonical `validate`, `render`, and fixture-only `send` lifecycle commands with JSON output and nonzero HELD/rejected exit codes.
- Added `memory-promote` with READY/ref-valid gate; provider adapters remain dependency-injected.
- Added `revise` (parent lineage) and explicit `gc --remove` orphan cleanup commands.
- Added `jobs` to expose persisted pending/failed request payloads for host LLM response entry.
- Added `repair` to rebuild a request from persisted payload + validation errors without touching succeeded jobs.
- Canonical `run` now accepts backend/LLM/strategy/enrichment/no-email/dry-run options; dry-run creates no DB state and host mode rejects explicit headless backend selection.
- 검증: CLI ingest/validate/render/send/memory/revise/gc/dry-run/JSON and storage schedule tests; 전체 offline suite `221 passed`.
- Added the remaining lifecycle commands — `status`, `resume`, `cancel`, `runs` — all wired to the existing canonical services (`storage.runs.RunStore.get/pending_jobs/transition`, `services.engine.Engine.resume`); no orchestration logic was duplicated in the CLI.
- `cancel` uses the CAS `transition` into `CANCELLED` and rejects terminal runs with `RUN_NOT_CANCELLABLE` (exit 21); `resume` rejects them with `RUN_NOT_RESUMABLE`; unknown run ids return structured `RUN_NOT_FOUND` (exit 2).
- Legacy ambiguity resolved: `date`/`hours` were both positional, so `prmonitor pre 24` silently parsed 24 as the report date. The dispatcher now accepts only the canonical `YYYY-MM-DD` date and rejects anything else with an explicit message pointing at `--hours` (exit 2). Legacy command *aliases* (pr-clip/pr-monitor/newsletter/…) keep their existing deprecation warning.
- 검증: `.venv/bin/python -m pytest tests -q -m 'not live'` → `242 passed` (new lifecycle/ambiguity coverage in tests/test_cli_v1.py).
- 미완료: none for T10. Live delivery/authenticated host wiring stays in T14.
