# T12 진행 기록

- 상태: done
- 구현: 공개 `docs/demo` 산출물에 대해 파일 allowlist/필수 페이지/source anchor 수를 검사하는 `scripts/demo/verify_samples.py`를 추가했다. 검사는 네트워크와 LLM 없이 committed fixture를 읽는다.
- `scripts/demo/build_samples.py`를 두 번 연속 실행해 산출물 diff가 없음을 확인했다(생성 로그는 fixture/local patch만 사용).
- viewport/accessibility: `verify_samples.audit()`가 stdlib `html.parser`만으로 각 페이지의 responsive viewport meta(`width=device-width`), `<html lang>`, 단일 `<h1>`·heading level skip 없음, `<img>` alt 텍스트, `<a href>`의 accessible name(본문 텍스트 또는 `aria-label`)을 검사한다. 헤드리스 브라우저·네트워크 없이 committed HTML만 파싱하며, 4개 페이지 모두 위반 0건이다.
- 검증: `tests/test_samples_v1.py` 및 전체 offline suite로 artifact 존재·source link 6건 이상·a11y 위반 0건을 확인한다. 검사가 무의미해지지 않도록 결함 있는 markup에 대해 audit이 실제로 실패하는 테스트도 함께 둔다. (`pytest tests -q -m 'not live'` → 245 passed)
- 미완료: 기존 builder의 mock 의존 제거. Pages 실제 배포 검증은 live 사이트가 필요하므로 본 티켓 범위 밖이며 T14(live)에서 다룬다.
