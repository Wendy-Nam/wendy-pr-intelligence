# T04 진행 기록

- 상태: done
- 구현: backend invocation/result contracts, bounded subprocess execution, Codex stdin invocation, generic argv-array/string template parsing을 추가했다.
- Claude/Hermes backend skeleton, structured process-start failure, timeout cleanup, and central model/backend role resolution을 추가했다.
- `--help` health probes now distinguish a present binary from a runnable interpreter; non-zero auth/unsupported-flag failures are classified as non-retryable and diagnostics redact common credentials.
- Claude/Codex/generic adapters now expose the bounded `Backend.execute()` contract over the common process runner; Hermes inherits generic execution only when an explicit template is configured.
- 검증: process/adapter tests and 전체 offline suite `204 passed` via `.venv/bin/python -m pytest tests -q -m 'not live'`.
- 미검증: authenticated live probes and Hermes headless remain intentionally unrun because no external credentials/environment were authorized.
