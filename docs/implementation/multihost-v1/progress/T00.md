# T00 진행 기록

- 상태: done
- 기준 commit: `99b1d80d27fa63140043068e4a933a5b462c6a2c`
- 현재 diff: 제품 코드 변경 없음. 작업 시작 전부터 `docs/architecture-audit/`와 `docs/implementation/`가 미추적 상태였으며 보존했다. 이 티켓의 변경은 이 기록과 `STATUS.json`뿐이다.
- 읽은 문서: `README.md`, `00-DECISIONS.md`, `STATUS.json`, `04-IMPLEMENTATION.md` T00, `05-VERIFICATION.md` V00, `docs/architecture-audit/REPORT.ko.md`.
- 완료한 함수/계약: 없음 (기준선 조사 티켓).
- 설계 검증: system Python의 `jsonschema 4.26.0`으로 `docs/implementation/multihost-v1/evidence/verify_plan.py`를 실행해 PASS를 확인했다. runtime `.venv`에는 아직 해당 패키지가 없어 CI requirements-dev 설치가 필요하다.
- 실행한 명령과 exit:
  - `git status --short`, `git log -1 --oneline`, `pwd` → exit 0; 기준 커밋은 설계 README의 baseline과 일치.
  - `.venv/bin/python -m pytest tests -q -m 'not live'` → exit 0, `134 passed in 7.28s`.
  - `.venv/bin/python docs/architecture-audit/reproduce.py` → exit 0; `ALL_SYNTH_FAILED`에서 briefing 존재/빈 내용/발송 경로 진입, `CACHE_EXPANSION`에서 stale synthesis context, `EMPTY_BRIEFING_POST`에서 `would_send: True`, no-email/dry-run argparse exit 2, generic argv quoting 손상이 재현됨.
  - `claude --version/--help`, `codex --version/--help`, `hermes --version/--help` → Claude Code `2.1.270`, Codex CLI `0.149.1`; Hermes launcher는 누락된 `/Users/namseoa/.hermes/hermes-agent/venv/bin/python`으로 exit 127. 인증·유료 LLM 호출은 하지 않았다.
  - `python3 docs/implementation/multihost-v1/evidence/verify_plan.py` → exit 0, `PASS: schema, request/result hashes, refs/coverage, article IDs, artifact hashes, local links, T00–T14 and V00–V94`.
  - `docs/demo` 정적 기준 확인 → market/self briefing은 `sources.html`의 각각 6개 source anchor를 사용하며, 두 HTML 모두 홈 링크와 DEMO 배너를 포함. mobile CSS는 600px 이하 padding 축소를 정의하지만 실제 390px 렌더 검증은 T12 범위다.
- 검증한 V IDs: V00.
- 다음 첫 명령: `sed -n '38,60p' docs/implementation/multihost-v1/04-IMPLEMENTATION.md` 후 T01의 F01–F08과 V01–V08에 지정된 함수와 테스트만 읽는다.
- 사용자/외부 의존성: 실제 Claude/Codex LLM 및 이메일은 실행하지 않았다. Hermes 환경은 broken launcher 상태이며 복구하지 않았다.
- 설계 변경 여부: 없음.
