# T07 진행 기록

- 상태: done
- `pipelines/pr.py`, pure CSV/XLSX exports, formula-safe cells, and deterministic PR record normalization implemented.
- `services/self_workflow.py` now provides a rules-first, explicit-article self report boundary with deterministic tone counts and no implicit backend/network calls.
- 레거시 self-brief wrapper 통합 완료: `prmonitor/steps/self_brief.py` 가 자체 건수 계산을
  버리고 canonical self workflow 만 호출한다.
  - F14 수정: `_count_csv_rows`(물리적 줄 수 = `wc -l` 포트)를 제거하고 `_self_report()`
    가 CSV 를 레코드 단위로 읽어 `services.self_workflow.prepare_self_report` 에 넘긴다.
    맥락 셀 안의 개행이 더는 PR 건수를 부풀리지 않으며, 그 건수가 이메일 제목·exec-log·
    engine ledger 에 함께 쓰인다.
  - 레코드 판정/집계는 `self_workflow` → `pipelines/pr.to_pr_records` 가 단독 소유한다.
    래퍼는 레거시 CSV 를 canonical 입력 형태로 맞추기만 한다.
  - `to_pr_records` 의 빈 alias scope 는 "입력이 이미 자사 범위로 좁혀졌다"는 뜻으로,
    이미 좁혀진 산출물을 재필터하지 않는다.
  - ledger result 에 canonical tone 분포를 함께 기록한다.
- `self_brief_daily` 는 그대로 `pre` + `self_brief` 오케스트레이터이며 date/hours/no_email 을
  그대로 전달하므로 동일 경로를 상속한다. CLI 계약(`self-brief`, `pr-clip`, `pr-monitor`,
  `self-brief-daily`, `pr-clip-daily`, `pr`) 변경 없음.
- 회귀: `tests/test_pr_pipeline_v1.py`, `tests/test_self_workflow_v1.py`
  (개행 포함 셀의 건수 / CSV 부재 시 0건).
- 전체 offline suite: `234 passed` (`.venv/bin/python -m pytest tests -q -m 'not live'`).
- 미검증: live email/authenticated host 경로는 T14 범위이며 여기서 실행하지 않았다.
