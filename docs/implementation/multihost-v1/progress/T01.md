# T01 진행 기록

- 상태: done
- 기준 commit: `99b1d80d27fa63140043068e4a933a5b462c6a2c`
- 현재 diff: T00 기록 외 `prmonitor/steps/llm_adapter.py`, `pre.py`, `market_brief.py`, `self_brief.py`, `self_brief_daily.py`, `__main__.py`를 수정했다.
- 읽은 문서: `04-IMPLEMENTATION.md` T01, `05-VERIFICATION.md` V01–V08, `00-DECISIONS.md` F01–F08, 지정 대상 함수와 관련 기존 테스트.
- 완료한 함수/계약:
  - Codex 기본 argv에서 0.149.1이 거부하는 `--full-auto` 제거.
  - generic CLI 템플릿을 tokenization 후 치환해 prompt의 공백/따옴표 분할을 막고, unknown placeholder를 `RuntimeError`로 표시.
  - 수집창 확대 시 synthesis-context와 enriched intermediate도 무효화.
  - canonical/alias CLI의 `--no-email`, market-brief의 `--dry-run` 등록 및 PR daily→PR pipeline 전달.
  - `self_brief`에서 no-email 및 `send_email:false` 시 발송 skip.
  - market dry-run을 경로 생성·pre·LLM·memory·email·exec-log 이전의 plan-only short-circuit으로 이동.
- 이번 세션 완료 항목:
  - required core/category JSON을 타입 검증하고, 하나라도 실패/손상되면 final merge를 만들지 않도록 변경했다.
  - final briefing의 필수 구조와 빈 TLDR을 post 이전에 거부해 nonzero로 종료한다.
  - glossary 기본 model은 Claude에서만 Claude 기본값을 쓰며 Codex/Hermes는 빈 model로 해당 CLI 기본값을 사용한다.
  - generic prompt-file은 subprocess `run`/`run_text` 종료 시 정리한다.
  - resolver 실패, quality-warning 파일 누락/손상/비배열은 발송 보류하고 landscape update를 건너뛴다.
  - `tests/test_pipeline_failures.py`, `tests/test_llm_adapter.py`를 추가했다.
- 실행한 명령과 exit:
  - `.venv/bin/python -m pytest tests/test_pipeline_failures.py tests/test_llm_adapter.py -q` → exit 0, `5 passed`.
  - `.venv/bin/python -m pytest tests -q -m 'not live'` → exit 0, `139 passed in 25.48s`.
  - `.venv/bin/python -m prmonitor market-brief --dry-run` → exit 0; plan-only message 확인.
  - `codex exec --help` → exit 0; `codex exec [--model MODEL] [PROMPT]`와 `--full-auto` 부재를 실제 설치 버전에서 확인.
  - `git diff --check` → exit 0.
- 검증한 V IDs: V01/V03 (invalid required core output), V02 (invalid category output), V04 (cache invalidation 코드 경로), V05 (resolver rc=1 held, email/memory update 0), V06 (no-email propagation), V07 (pre-stage plan-only), V08 (argv tokenization, prompt-file cleanup, Codex help).
- 다음 티켓: T02 — `04-IMPLEMENTATION.md`의 T02가 지정한 계약·경로·저장 문서/함수만 읽는다.
- 사용자/외부 의존성: 실제 LLM·메일은 실행하지 않았다.
- 미검증 범위: 실제 Claude/Codex/Hermes headless 호출과 실제 이메일은 실행하지 않았다. 외부 LLM/메일 비용 및 발송 권한이 이 세션에 없으므로 fixture/local mock으로만 검증했다.
- 설계 변경 여부: 없음.
