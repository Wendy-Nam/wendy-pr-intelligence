# T02 진행 기록

- 상태: done
- 읽은 범위: `01-ARCHITECTURE.md` §3–7, `02-CONTRACTS.md` §1–3/8–9/12, examples, 그리고 T02 지정 대상 파일만 읽음.
- 구현 시작:
  - `models.py`, `errors.py`에 host-agnostic `RunSpec`, `RunState`, `PathContext`, canonical JSON hash와 typed error를 추가했다.
  - `paths.resolve_paths()`를 추가해 explicit workspace/cache와 workspace namespace를 해석한다. 기존 global path shim은 호환성을 위해 유지했다.
  - `storage/database.py`, `storage/runs.py`, `storage/artifacts.py`에 SQLite run 원장, CAS transition, atomic JSON artifact write 및 DB-backed hash verification의 초기 구현을 추가했다.
- 추가 구현:
  - `RunSpec.from_dict()`와 bundle-local `contracts/run-spec.schema.json`을 추가했다.
  - injected `load_domainpack()`은 operational mode에서 사용자 domain pack 누락을 오류로 처리하며 template fallback은 explicit example mode에서만 허용한다.
  - `write_manifest()`은 DB에서 projection을 재생성하며 lease owner+expiry 기반 `acquire_lease()`/`release_lease()`를 추가했다.
  - `tests/test_storage_v1.py`로 V09/V10/V12/V13/V14/V15/V16의 핵심 로컬 경로를 검증했다.
- 현재 검증: `tests/test_storage_v1.py` → `4 passed`; 전체 `.venv/bin/python -m pytest tests -q -m 'not live'` → `143 passed`; `git diff --check` 성공.
- SQLite migration에는 jobs/validations/deliveries/memory ledger tables를 포함했다. manifest doctor는 `ArtifactStore.orphan_paths()`를 integrity reader로 제공하며 orphan를 자동 신뢰/승격하지 않는다.
- 다음 티켓: T03 — 수집·캐시. 실제 LLM·메일·공개 배포는 실행하지 않았다.
- 외부 실행: LLM·메일·공개 배포를 실행하지 않았다.
- `RunSpec.validation_errors()` exposes pipeline/mode/timezone/window/budget/backend contract diagnostics without breaking legacy record decoding.
- SQLite jobs now persist the canonical request payload (migration 3), and `RunStore.pending_jobs()` exposes only pending/failed requests without reconstructing them from hashes.
- 현재 전체 offline suite: `225 passed`.
