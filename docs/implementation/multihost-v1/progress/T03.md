# T03 진행 기록

- 상태: done
- 읽은 범위: `01-ARCHITECTURE.md` §8, `02-CONTRACTS.md` Article/CollectionReport, `03-WORKFLOWS.md` W04, T03 및 V17–V23.
- 구현: `pipelines/articles.py`의 URL normalization/안정 article ID/충돌 검출, `storage/cache.py`의 exact-key hash-verified stage cache, `services/collection.py`의 all-source-failed와 true NO_DATA 구분을 추가했다.
- 기존 `aggregate.py`와 `enrich-articles.py`의 MD5 6자리 ID를 공통 canonical SHA-256 article ID로 교체했다.
- `pipelines/context.py`에 self-context snapshot까지 hash에 결합하는 immutable context builder를 추가했다.
- StageCache의 `force_refresh` 경로는 동일 key라도 producer를 재호출하게 했다(기존 성공 cache 삭제 없음).
- `pipelines/classify.py`에 profile/keywords/tuning snapshot을 명시 입력으로 받는 결정론적 classifier를 추가했다.
- `pipelines/extract.py`에 UTC collection window를 적용하는 extraction normalization을 추가했다.
- 검증: `tests/test_collection_v1.py` 9 passed; 전체 offline suite 153 passed.
- 검증: `tests/test_collection_v1.py` 5 passed; 전체 offline suite 149 passed; `git diff --check` 성공.
- Legacy scripts remain as compatibility wrappers during migration; new v1 services use explicit-input library surfaces. 외부 network/LLM/email은 실행하지 않았다.
