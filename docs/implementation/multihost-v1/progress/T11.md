# T11 진행 기록

- 상태: done
- 구현: `scripts/packaging/build.py`에 Claude/Codex/Hermes별 allowlist staging builder를 추가했다. 공통 `prmonitor/**` 파일 목록에서 core hash를 계산하고 `.git/.venv/.prmonitor/data/tests/docs`를 배제한다. Codex/Hermes manifest는 각 artifact에만 생성한다.
- 검증: `tests/test_packaging_v1.py` (세 host의 동일 core content hash) 및 전체 offline suite `202 passed`.
- Host-neutral `market-brief`, `pr-monitor`, `pr-setup` skill surfaces와 Hermes native `register(ctx)` shim을 추가했다. 등록 함수는 skill 파일만 읽고 설치/호출하지 않는다.
- `scripts/packaging/probe_hosts.py`로 설치된 Claude 2.1.270/Codex 0.149.1 help와 Hermes missing-interpreter 상태를 실제 로컬 환경에서 확인했다. 인증/모델 호출은 하지 않았다.
- Bundle validator와 isolated `PYTHONPATH` smoke import를 추가해 host artifact가 공통 core를 실제로 import할 수 있는지 확인한다.
- 전체 offline suite: `211 passed`.
- Probe helper regression covered; 전체 offline suite `219 passed`.
- `scripts/packaging/build.py`에 `load_installed_host(root)`를 추가했다. 설치된 bundle의 on-disk manifest(`.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, `plugin.yaml`)만 읽어 host 종류·name/version·설치된 skill 목록을 파싱하고 `validate_bundle` 결과를 함께 돌려준다. manifest 부재/중복/파손은 오류 대신 구조화된 결과로 보고한다. 파일시스템만 읽으며 host 프로세스를 실행하거나 연결하지 않는다.
- 검증: `tests/test_packaging_v1.py`의 fixture 기반 parser 회귀 2건 추가, 전체 offline suite `244 passed`.
- 범위 밖(T14): Hermes native registration(실제 host 런타임 등록)과 live-host install dry-run은 실행 중인 host가 필요하므로 T11에서 다루지 않는다. release artifact 서명도 미완료.
