# T13 진행 기록

- 상태: done
- 구현: `.github/workflows/offline-tests.yml`에 OS × Python matrix(`ubuntu-latest`/`macos-latest` × `3.11`/`3.12`, 4 job)를 확정했다. CI는 실제 LLM/email 자격증명을 요구하지 않고 `not live` marker만 실행한다. 오프라인으로 검증할 수 없는 Windows runner는 matrix에서 제외했다(T14 live 범위).
- HELD/unknown delivery/config recovery/release checklist를 `docs/operations/multihost-v1-runbook.md`에 문서화했다.
- `services/offline.py` fixture 실행으로 prepare→ingest→validate→render→LocalTransport send 전체 경로를 CI에서 재현할 수 있게 했다(`tests/test_offline_e2e_v1.py`).
- 문서 audit(T05–T12 구현 상태 대조):
  - HELD 복구 절차를 T10이 추가한 실제 lifecycle 명령(`jobs`/`repair`/`resume`/`status`/`runs`/`cancel`/`revise`)으로 교체했다. 기존의 "pending job만 재실행" 서술은 대응 명령이 없던 시점의 표현이었다.
  - config 복구 항목을 T09 구현과 일치시켰다. `migrate_config`는 레거시 이관 전용이며 target이 있으면 `preserved_existing`이므로, 기존 v1 config에 템플릿 신규 키를 채우는 경로는 `upgrade_config(..., dry_run=True)`임을 명시했다.
  - release checklist의 sample 검증을 `.venv/bin/python scripts/demo/verify_samples.py docs/demo`로 고치고 T12가 추가한 viewport·접근성 검사 범위를 밝혔다.
- 검증: `.venv/bin/python -m pytest tests -q -m 'not live'` → `245 passed`; `scripts/demo/verify_samples.py docs/demo` → exit 0; `git diff --check` 통과.
- 미완료(T14 범위): Windows runner, 실제 host install/load, live release gate, live email.
