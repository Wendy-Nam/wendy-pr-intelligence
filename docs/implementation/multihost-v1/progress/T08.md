# T08 진행 기록

- 상태: done
- 구현: local fixture transport, PASS/hash/example-mode delivery gate, stable dedupe key, provenance candidate/READY promotion gate를 추가했다.
- SQLite delivery reservation 및 sending→accepted/failed/unknown 단방향 전이를 추가했다.
- 검증: delivery/memory 테스트 포함 전체 offline suite `194 passed`.
- transport 예외는 `unknown`으로 원장에 기록하고 자동 재전송하지 않으며, receipt_json을 보존한다.
- SQLite `memory_events` ledger에 stable event idempotency와 READY/ref-valid 단방향 promotion을 추가했다.
- 전체 offline suite: `202 passed`.
- Canonical `send` CLI enforces READY + validation hash gates and writes only to `LocalTransport` fixture storage in offline mode; accepted/non-ready cases are tested.
- SMTP and Microsoft Graph adapters now require an explicitly injected sender callable; they cannot open sockets or read secrets merely by import/construction. `memory-promote` CLI enforces READY/ref-valid gates.
- 전체 offline suite: `215 passed`.
- `resolve_transport`(delivery_providers.py)가 config/env(`PRMONITOR_DELIVERY_PROVIDER`)·`send --provider`로 transport를 선택하며, 기본값은 offline LocalTransport다. smtp/microsoft_graph는 선택만 가능하고 sender 주입 없이는 `*_NOT_CONFIGURED`로 실패한다 — 소켓/크리덴셜 경로 없음.
- `send` CLI가 하드코딩된 LocalTransport 대신 resolver를 사용한다. 알 수 없는 provider는 `UNKNOWN_DELIVERY_PROVIDER`(rc 21).
- 범위 밖: 실제 live 전송 배선(SMTP/Graph 크리덴셜·네트워크)과 운영 unknown resolve 명령은 T14에서 다룬다.
- 전체 offline suite: `235 passed`.
