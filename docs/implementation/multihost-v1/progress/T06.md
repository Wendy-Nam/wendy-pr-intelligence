# T06 진행 기록

- 상태: done
- 구현: deterministic briefing validator 및 PASS 이후 briefing/policy/HTML hash 재확인 delivery guard를 추가했다.
- `ValidationReport.as_dict()`와 렌더 텍스트/URL safety primitives(`render_safety.py`)를 추가했다.
- Engine facade now persists validation rows and transitions a run to READY only on PASS, otherwise HELD; validation/render safety tests plus full suite are `201 passed`.
- Canonical `validate` CLI now emits a report artifact and returns exit 0/21 for PASS/HELD.
- Canonical `render` CLI refuses HELD unless `--allow-held`, and marks held previews with the review watermark.
- 전체 offline suite: `208 passed`.
- 레거시 뉴스레터 post wrapper 통합 완료: `prmonitor/steps/post.py` 가 자체 게이트를 버리고
  canonical 서비스만 호출한다.
  - `_canonical_briefing()` 는 레거시 briefing(`insight.facts[].ref`)을 validator 형태
    (`insight.refs`)로만 얕게 변환하고, schema/ref/category/coverage 판정은
    `validation.validate_briefing` 이 단독 소유한다. 검증 실행 자체가 실패해도 통과가
    아니라 보류다.
  - 발송 직전 `deliverable_is_current` 로 briefing(디스크 재독)·policy·HTML hash 를
    재확인한다 — 검증 이후 바뀐 원고는 발송하지 않는다.
  - 보류 확정 시 산출 HTML 에 `render.HELD_WATERMARK` 를 붙인다(문자열은 canonical
    renderer 와 공유 — 표식 중복 정의 제거).
- 회귀: `tests/test_post_validation_v1.py` (정상 발송 / unknown ref 보류+워터마크 /
  검증 이후 변조 시 발송 차단).
- 전체 offline suite: `232 passed` (`.venv/bin/python -m pytest tests -q -m 'not live'`).
