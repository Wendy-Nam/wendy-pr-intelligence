# T09 진행 기록

- 상태: done
- 구현: `services/setup.py`에 네트워크/설치 부작용 없는 Python 런타임 fingerprint와 원자적 ready marker를 추가했고, requirements 변경 시 marker mismatch를 감지한다.
- `doctor.inspect_workspace()`는 Python/schema/state writable와 orphan 상태를 구조화하고, Claude/Codex는 실제 `--help` probe, Hermes는 configured template 부재를 unsupported로 반환한다. delivery configured는 실제 secret을 읽지 않고 false로 명시한다.
- `migrate_config()`는 dry-run/backup-safe semantics로 기존 사용자 설정을 덮어쓰지 않고 legacy→target 복사 계획을 적용한다.
- `doctor --strict`는 orphan/schema/python/state 오류가 있으면 구조화 JSON을 출력하면서 exit 1을 반환한다.
- `init --strict`는 dependency/venv 준비 실패를 숨기지 않고 exit 20으로 반환하며, 기본 SessionStart 경로는 기존처럼 세션을 살린다.
- `upgrade_config()`는 기존 사용자 `config/runtime.yaml`에 번들 템플릿에서 새로 추가된 키만 채워 넣는다. mapping-only deep merge라 사용자 값과 리스트는 항상 보존되고, 최초 1회 `.bak` 백업 후 원자적으로 교체하며, 재실행 시 `current`(no-op)로 멱등하다. dry-run은 추가될 키 경로만 반환한다.
- `steps/init.py`는 시드 단계에서 이미 존재하는 `runtime.yaml`에 대해 `upgrade_config()`를 호출해 legacy 워크스페이스를 현재 스키마로 끌어올리고, 보강된 파일명을 stderr에 보고한다. 도메인팩(categories/sources 등)은 사용자 콘텐츠이므로 병합 대상에서 제외한다.
- 검증: setup/doctor/migration/init strict 포함 전체 offline suite `238 passed` (`.venv/bin/python -m pytest tests -q -m 'not live'`).
- config migration은 종료됨: 신규 키 채움/사용자 값 보존/백업/멱등/타깃·템플릿 부재 no-op, 그리고 init 경유 end-to-end까지 `tests/test_setup_migration_v1.py`에서 오프라인으로 커버한다.
- 남은 환경 의존 항목(티켓 범위 밖, 오프라인 테스트 불가): 실제 `pip install` 기반 dependency 설치/교체/rollback. 런타임 fingerprint와 ready marker로 불일치 "감지"까지만 오프라인에서 보장하고, 설치 자체는 환경에서 수행한다.
