---
name: pr-setup
description: Initialize and diagnose a PR Intelligence workspace.
---

Run `prmonitor init --force` only for explicit setup, then `prmonitor doctor`. Do not install dependencies or alter user config from a host skill without an explicit setup action.
